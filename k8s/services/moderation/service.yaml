apiVersion: v1
kind: Service
metadata:
  name: moderation-svc
  namespace: chirp-prod
  labels:
    app: moderation
spec:
  type: ClusterIP
  selector:
    app: moderation
  ports:
    - port: 8010
      targetPort: 8010
      protocol: TCP
