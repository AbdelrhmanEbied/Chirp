apiVersion: v1
kind: Service
metadata:
  name: search-svc
  namespace: chirp-prod
  labels:
    app: search
spec:
  type: ClusterIP
  selector:
    app: search
  ports:
    - port: 8006
      targetPort: 8006
      protocol: TCP
