apiVersion: v1
kind: Service
metadata:
  name: media-svc
  namespace: chirp-prod
  labels:
    app: media
spec:
  type: ClusterIP
  selector:
    app: media
  ports:
    - port: 8009
      targetPort: 8009
      protocol: TCP
