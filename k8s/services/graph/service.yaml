apiVersion: v1
kind: Service
metadata:
  name: graph-svc
  namespace: chirp-prod
  labels:
    app: graph
spec:
  type: ClusterIP
  selector:
    app: graph
  ports:
    - port: 8004
      targetPort: 8004
      protocol: TCP
